var gulp = require("gulp"),
    rimraf = require("rimraf"),
    config = require('../config');


// Clean all wwwroot src directories
gulp.task("clean_all", ["clean_css", "clean_js"], function () {
    
});